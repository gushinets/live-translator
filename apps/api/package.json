{
  "name": "@live-translator/api",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env ./node_modules/tsx/dist/cli.mjs watch src/server.ts",
    "build": "tsc -p tsconfig.build.json && node scripts/copy-migrations.mjs",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.0.0",
    "express-rate-limit": "^8.7.0",
    "openai": "^7.15.0",
    "ws": "^8.21.0",
    "zod": "^4.6.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "@types/supertest": "^6.0.0",
    "@types/ws": "^8.5.13",
    "supertest": "^7.0.0",
    "tsx": "^4.0.0"
  }
}
